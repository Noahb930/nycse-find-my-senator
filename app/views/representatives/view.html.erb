<% @representatives.each_with_index do |representative, index| %>
  <div id = "representative_container_<%= index %>">
    <%= render :partial => 'show', :locals => {:@representative => representative, :@index => index} %>
  <div>
<% end %>
